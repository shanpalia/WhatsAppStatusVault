# Deleted Message Card V8

The existing Removed Messages card now surfaces previously captured WhatsApp
message text when WhatsApp emits a deletion/retraction indicator for the same
conversation. The app does not fabricate the original text if it was never
captured in a notification.

Notification removal is also retained as removal evidence, but no fake deleted
message content is generated.
