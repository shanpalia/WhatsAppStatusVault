# One-time WhatsApp Status Access

This build no longer requires manually selecting the `.Statuses` folder.
On Android 11+, the app requests Android's All files access once. After the user enables it, the app scans the standard WhatsApp and WhatsApp Business status directories automatically.

Android still requires the user to approve this special access in Settings; an app cannot silently grant it to itself.
