# Status Count Card V7

The total/image/video counts are now displayed together inside one centered
Card instead of three separate cards/tiles. The header no longer duplicates
the image/video counts.
